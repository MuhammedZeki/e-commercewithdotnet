@{
    Layout = "_AdminLayout";
}
@model AppUser
<div class="row">
    <div class="col">
        <div class="card">
            <div class="card-body text-center">
                @Model.FullName isimli Kullanıcıyı silmek istiyor musunuz?
                <form asp-controller="User" asp-action="DeleteConfirmed" asp-route-id="@Model.Id" method="post">
                    <input type="hidden" name="Id" value="@Model.Id">
                    <button type="submit" class="btn btn-danger">Evet</button>
                    <a asp-action="Index" type="button" class="btn btn-primary">Hayır</a>
                </form>
            </div>
        </div>
    </div>
</div>